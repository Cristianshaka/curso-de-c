#include <stdio.h>

int main() {
 int idades = 0, cont = 0;
 
 while (idades != -1 ){
     
  
    printf("Digite a idade: "); 
    scanf("%d", &idades);
    
    if(idades >= 18)
    cont ++;
  
 } 
 printf("%d", cont);
}
